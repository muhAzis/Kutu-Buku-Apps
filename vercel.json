{
  "buildCommand": "cd kutu-buku && npm install && npm run build",
  "outputDirectory": "kutu-buku/build",
  "framework": "create-react-app",
  "rewrites": [
    {
      "source": "/api/(.*)",
      "destination": "/api/index.js"
    }
  ]
}
